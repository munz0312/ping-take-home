services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ping_dev
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: ping_residential
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro

volumes:
  postgres_data:
